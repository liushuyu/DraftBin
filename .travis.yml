language: python
python:
   - "3.5"
   - "3.5-dev"
   - "nightly"

sudo: required
script:
   - sudo apt-get update -qq
   - wget "https://bootstrap.pypa.io/get-pip.py"
   - sudo python get-pip.py
   - sudo pip install octohub
   - python pyscripts/test-fr.py
